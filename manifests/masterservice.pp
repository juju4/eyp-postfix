define postfix::masterservice(
                                $service_type,
                                $type,
                                $private = true,
                                $unpriv = true,
                                $chroot = 'n',
                                $wakeup = '-',
                                $maxproc = '-',
                                $command_args,
                                $comment = undef,
  ) {

  #service type  private unpriv  chroot  wakeup  maxproc command + args

}
